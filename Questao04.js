// Criação da variável e atribuição do valor
let temperatura = 30;  // Variável 'temperatura' com valor 30

// Verificação da temperatura e exibição da mensagem
if (temperatura > 25) {
    console.log("Está calor!");
} else {
    console.log("Está fresco!");
}